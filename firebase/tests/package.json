{
  "name": "@indigen-world/firebase-tests",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Emulator-based Firestore Security Rules tests for the Indigen World data layer.",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.3.0",
    "firebase-admin": "^14.2.0"
  }
}
